# IqUp-Web — Decisions Log

Append-only record of project decisions. Newest entries go at the bottom. Each entry: date, the decision, and a short "why."

---

## Conventions

- **Append, don't edit.** Never rewrite or delete a past entry.
- **Reversing a decision?** Add a *new* entry that states the change and references the entry it replaces (e.g. "Supersedes 2026-06-07 #6").
- **Who appends:** Chat logs decisions made during planning and chat. Code appends any decision it had to make on its own during a phase — and also surfaces it in that phase's completion report so Chat can flag it to Lazar.

---

## Decisions

### 2026-06-07 — Seeded from intake + planning

1. The site is a marketing-campaign funnel, not a multi-page brochure site. *Why: the goal is leads + awareness, not a full company website.*
2. Honest framing — an IQ-test hook for the headline, but results are a strengths profile, never a clinical IQ number. *Why: a valid IQ score for young children online isn't possible or honest; protects parent trust and keeps us legally and ethically sound.*
3. The test covers ages 3–13, even though IqUp's program is 3–9. *Why: a wider net for leads and awareness.*
4. Three age bands — 3–5 (parent-assisted), 6–9 (mostly solo), 10–13 (solo). *Why: balances age-fit against the amount of content to write.*
5. The parent enters their email *before* results are shown. *Why: maximizes lead capture, the primary goal.*
6. Results = strengths profile + shareable certificate for all three bands; a free-trial invite for 3–5 and 6–9; 10–13 ends at the result. *Why: the certificate drives sharing and awareness; there's no program for 10–13, so no trial CTA.*
7. Bibi characters — existing licensed images only, never generated or redrawn. *Why: IP/license correctness; we don't recreate licensed characters.*
8. Test questions are original, inspired by general task types, never copied from a proprietary test. *Why: copyright safety.*
9. Languages — Macedonian default at `/`, English at `/en/`. *Why: North Macedonia market, MK-first.*
10. Stack locked — Next.js / TypeScript / Tailwind / shadcn-ui / Framer Motion / Lucide / next-intl, Supabase (EU), GA4 + Microsoft Clarity + Meta Pixel, Vercel, iubenda/Termly. *Why: fits a fast, interactive, bilingual lead funnel.*
11. Leads stored in Supabase, EU region. *Why: already connected, free tier, GDPR-appropriate data location.*
12. Two-part build — Part 1 builds locally, Part 2 integrates and launches. *Why: clean separation of building vs. going live.*
13. Two Design phases (foundation; results + certificate). *Why: iterate the look before locking it into code.*
14. Deferred to Part 2 — email service, CRM, trial-booking mechanic. Deferred to launch — domain + DNS. *Why: not needed for the local build.*
15. Repo `DinovLazar/iqup-web`, private. Local folder `C:\Users\user\Desktop\iqup-web`. *Why: client work.*
16. Replaced the usual Notion checklist with `CLAUDE.md` + `AGENTS.md`. *Why: agent-facing repo instructions are more useful for a Code-heavy build.*
17. Phase 1.01 is a Chat research phase producing `brand.md`. *Why: establish the brand source-of-truth before any building.*
18. No AI features at launch. *Why: rule-based scoring with templated strengths results is safer and more predictable for children than AI-generated copy.*

### 2026-06-07 — Phase 1.02 scaffold (Code)

19. **npm** is the package manager for the whole project (single `package-lock.json`). *Why: one lockfile, one toolchain; matches the scaffold brief and avoids mixed-manager drift.*
20. next-intl `localePrefix: 'as-needed'` — Macedonian served at `/` with no prefix, English at `/en`. *Why: MK is the default market locale and should own the clean root URL; only the secondary locale carries a prefix.*
21. Code self-reviews and verifies its own work each phase (via the `requesting-code-review` and `verification-before-completion` skills). Heavier external AI review (CodeRabbit / Codex) and branch-protection rules are **deferred** to a future pull-request workflow. *Why: keeps the solo local build fast now; formal PR review can be layered on when the repo opens up to more contributors.*
22. Repo structure follows `plan.md` §9, **not** the generic `project-repo-bootstrap` skill layout (`briefs/`, `reports/`, `status/`, `docs/architecture`, CodeRabbit/Codex PR setup). *Why: this project has its own canonical structure and doc set (`src/_project-state/`, `docs/design-handovers/`, `AGENTS.md` + `CLAUDE.md`); the brief explicitly overrides the bootstrap skill.*

### 2026-06-07 — Phase 1.05 leads table + Supabase wiring (Code)

23. **The project starts on Lazar's personal Supabase account — transfer ownership to an IqUp-controlled account before launch.** *Why: convenient to build now; real parent data only arrives at launch, so the transfer must happen before then. (Carried over from the Cowork sub-phase.)*
24. **Supabase region `eu-central-1` (Frankfurt).** *Why: EU-hosted data residency for children's/parent PII, satisfying the GDPR requirement.*
25. **Summary-only storage — the `leads` table never stores a child's individual answers,** only the computed `top_strengths` summary. *Why: GDPR data minimization; the raw answers are not needed downstream.*
26. **All lead inserts are server-side only via the service-role key; RLS is enabled on `leads` with no anon/authenticated policies (plus a defense-in-depth `revoke`), so the public anon key can neither read nor write leads.** *Why: protects children's PII — the anon key ships in client code and must not be able to dump or write leads. Verified live (anon gets `permission denied`).*
27. **Separate, optional `marketing_opt_in` field, distinct from the required `consent`.** *Why: parental consent to submit is mandatory and must be `true`; a newsletter opt-in is a separate, optional choice used in Part 2. Final consent/marketing wording is IqUp's legal sign-off; `consent_version` records which wording was shown.*
28. **Plain `@supabase/supabase-js` (not `@supabase/ssr`).** *Why: no Supabase Auth/sessions are used; a stateless service-role client + plain anon client are correct and simpler.*
29. **zod 4 with a deliberate strip/strict split:** `leadSchema` strips unknown keys (zod-4 default, verified) so the lead is still saved cleanly if an anti-spam honeypot field rides along; `topStrengthsSchema` is `.strict()` with a number-only `scores` map so raw answers cannot be smuggled into the summary. *Why: balances forgiving form handling against strict data-minimization on the one field that could carry raw answers.*
30. **Migration applied via the Supabase dashboard SQL editor (browser), and `types.ts` hand-authored + verified against the live schema,** rather than `supabase db push` / `gen types`. *Why: the build sandbox cannot reach the Postgres port; the dashboard editor was the planned fallback and the types were verified column-by-column and proven by the passing live test. Regenerate types via `npm run db:types` once linked from an environment with DB access.*

### 2026-06-08 — Phase 1.06 landing page (Code)

31. **Age picker = exact-age chips grouped by band (not band cards), and the Start CTA carries `age` (not `band`).** The 1.03 handover §B.4 specified three band *cards*; the 1.06 phase prompt, its Definition of Done, plan.md §13, and the 1.08 email gate all require the **exact child age**. Reconciled by rendering an accessible radiogroup of ages 3–13 visually grouped under the three band labels (big targets, band-aware as the handover intends); the band derives from the age via `getBandForAge`. *Why: honours both the handover's UX intent and the carry-the-exact-age requirement (the leads table stores the child's age; the email gate reuses it).*
32. **Canonical band keys `3-5` / `6-9` / `10-13`** defined in `src/lib/bands.ts`. *Why: the 1.04 content spec was absent, so these become the project-wide source of truth; 1.07/1.08 must adopt them.*
33. **The full 1.03 design foundation (brand tokens + Rubik/Nunito Sans fonts) was wired in 1.06**, and the dead scaffold `.dark` block removed. *Why: 1.06 is the first build phase that needs the foundation — there is no separate "foundation build" phase. The palette/type are the handover's PROVISIONAL placeholders, referenced only via tokens so the real IqUp brand files re-skin by editing `globals.css` + the two `next/font` calls.*
34. **Vitest is the project unit-test runner.** *Why: none existed; needed for `bands.test.ts`; standard for the stack; reused by 1.07 scoring.*
35. **Client islands (`AgeStart`, `LanguageToggle`) receive their copy as props (resolved server-side) instead of calling `useTranslations` client-side; Framer Motion is loaded via `LazyMotion`; entrance animation is trimmed and kept off the LCP path; the body font is not preloaded.** *Why: keep the client bundle and main-thread work small and entrance motion within the performance budget (the locked stack's framework + brand-web-font baseline still leaves mobile Lighthouse Performance ~87 under throttling — documented for the 1.11 perf sweep).*
36. **Dynamic per-locale OG image via `next/og` (Cyrillic Rubik from `@fontsource/rubik`), no static fallback.** *Why: the Cyrillic glyphs render correctly (verified visually), so the preferred dynamic approach was kept rather than a static placeholder.*
37. **Placeholders, never generated:** `HeroArt` is an abstract decorative stand-in for the missing licensed Bibi art, and `Wordmark` is a token-styled `IQ UP!` stand-in for the missing official logo. *Why: the assets weren't in the repo and the rules forbid generating/redrawing Bibi characters; both are flagged for swap-in.*

### 2026-06-08 — Phase 1.07 test engine (Code)

38. **Content is keyed by the canonical `BandKey` (`3-5`/`6-9`/`10-13`), with the spec's `band-a/b/c` mapped 1:1 onto them; question ids use a short `a-q01`/`b-q01`/`c-q01` prefix tracing to the spec's item labels (A-Q01, …).** *Why: the phase prompt requires reusing `src/lib/bands.ts` and never redefining the bands; the letter prefix keeps each item traceable to spec §5 without re-introducing the `band-a` vocabulary as a band definition.*
39. **The content schema (spec §4) was extended with a typed `GlyphSpec`/`StemSpec` visual model, so every stem graphic and image option is *data* rather than per-question bespoke JSX.** *Why: the spec invites Code to own the final implementation; data-driven visuals keep the banks self-describing and let the content-integrity tests assert structure.*
40. **The shared strengths module carries the six codes, the 1.03 colour-token binding (`words_obs`→`verbal`), the bilingual display *names* (spec §1), and an English-only `whatItIs`.** The warm bilingual *celebrated/growing* blurbs (spec §6) are deliberately NOT included. *Why: spec §1 provides bilingual names but only an English "what it is"; the §6 blurbs are parent/child result copy owned by Phase 1.10 (`src/content/results/`), so duplicating them here would pull 1.10 scope (and unreviewed MK) into the taxonomy.*
41. **A tokenised `--toy-*` puzzle palette was added to `globals.css`** (red/blue/yellow/green/purple/orange/pink/teal/neutral) for the test graphics, each rendered with a shared `--ink` outline. *Why: a "red circle" must read as red — these are *content* colours, not chrome — but kept as tokens so no literal hex appears in components.*
42. **End-of-test hand-off persists the computed `TestResult` to `sessionStorage` under `iqup.testResult.v1`; `completedAt` is stamped at hand-off, NOT inside `score()`.** *Why: `sessionStorage` (not the URL/localStorage) keeps child data out of the address bar while surviving the navigation into 1.08; keeping the timestamp out of pure scoring preserves determinism for the tests.*
43. **Added a calm-play Start screen and a clearly-temporary Completion view (handover §D), and the `TestResult` contract = `{version, band, locale, strengths[](code/total/hits/ratio/rank/tier), top1/top2/top3, growing[], completedAt?}` with NO total/IQ field.** *Why: the runner flow benefits from a start state that sets the "no right/wrong" tone; the phase ends at "test complete → result handed off"; the contract is what 1.08/1.10 consume.*
44. **Dev preview = `?dev=1` gated to `NODE_ENV !== 'production'`:** a `DevBar` (jump to any band + auto-finish correct/mixed/wrong) plus a dev-only strengths summary on the completion view. *Why: lets all three bands + the computed result be verified without answering 36 questions; forced off (a no-op) in production.*
45. **Runner motion uses `tw-animate-css` `animate-in` utilities (already in the stack) for question entrances, and framer-motion's `useReducedMotion` hook only to pick the timed-vs-manual reveal path — no `MotionProvider`/`LazyMotion` in the runner; no new deps.** *Why: CSS-based entrances respect the global `prefers-reduced-motion` reset automatically and avoid LazyMotion strict-mode coupling; the reveal timer genuinely needs to branch on reduced motion.*
46. **Vitest gained a `@/*`→`src/*` alias (`vitest.config.ts`).** *Why: the new content/scoring modules use the app's `@/` imports; Vitest doesn't read tsconfig paths on its own.*
47. **The runner column is `max-w-xl` (~576px) rather than the handover §D's 420px.** *Why: a 420px column can't hold the 3-column image-option grids on desktop; mobile stays full-bleed 2-col. Minor, usability-driven deviation from the handover.*
48. **Object icons come from Lucide (endorsed by spec §7); the abstract puzzle figures and the few objects Lucide lacks (duck, sock, shoe, butterfly, ball, balloon, block) are original lightweight inline SVG. Image stems get a locale-aware generated text alternative (`stemAlt`).** *Why: leverages the locked icon set for recognisable objects while keeping full control of the puzzle-critical figures; the generated alt (draft MK) satisfies WCAG 1.1.1 for the graphic stems.*

### 2026-06-09 — Phase 1.08 email gate + lead capture (Code)

> Numbering note: the 1.08 brief said "continue from #48", but the log already had 48 entries (the last is #48 above), so the email-gate decisions start at **#49**.

49. **The exact child age is carried into the email gate, not re-collected.** Added an `age` prop to `TestRunner`, threaded from the `?age=N` the `/test` page already validates; the lead's `child_age` comes from it. *Why: `plan.md` §13 stores the child's age on the lead and the gate must submit it, but re-asking the age in a "grown-up step" form is redundant and error-prone — the test already knows it.*
50. **Band-vocabulary mapping made explicit in the lead path** (`LEAD_BAND_BY_KEY` in `src/lib/leads/lead-mapping.ts`: `3-5→band-a`, `6-9→band-b`, `10-13→band-c`). *Why: the canonical band keys are `3-5/6-9/10-13` (`bands.ts`) but the `leads` table predates them and its `band` CHECK is `band-a/b/c`; this is the same 1:1 youngest→oldest correspondence as decision #38, localized to the lead boundary so neither the bands module nor the schema is redefined.*
51. **The lead's `top_strengths.scores` = each strength's per-strength accuracy ratio (0–1, rounded to 2 dp), for all six codes.** *Why: a normalized "how strong" figure per strength is a complete, number-only strengths summary; it carries no question-level data and no aggregate/IQ/total, honouring decisions #25/#29 and the `.strict()` number-only `topStrengthsSchema`.*
52. **Submit path: a typed `GateSubmission` from the client → a `'use server'` `submitLead` action → honeypot check → existing service-role `insertLead()`.** The client passes a typed payload (not raw `FormData`) and the action re-validates everything with the existing `leadSchema`. *Why: the lead pipeline (`insertLead`/`leadSchema`/service-role) is already built and verified (1.05) and must not be reimplemented; a typed payload + server re-validation keeps the one write path intact and never trusts the client.*
53. **`CONSENT_VERSION = 'v1-draft-2026-06'`** is stamped on every lead, tied to the provisional consent wording. *Why: `consent_version` must record which wording the parent saw; the wording is a draft awaiting IqUp legal, so the version is explicitly a draft and must be bumped (with a new consent string) when the wording is finalised.*
54. **Temporary `/[locale]/result` + an `iqup.leadContext.v1` sessionStorage hand-off.** On a successful submit the gate persists a minimal `{childFirstName, age, submittedAt}` (no email, no strengths) and navigates to `/result`; `/result` redirects home when either that context or the persisted `TestResult` is missing. *Why: the funnel must be complete end-to-end now, but the real results screen is 1.10; the lead-context's presence is the "gate completed" signal, so the guard makes `/result` reachable only after lead capture (protecting it) and falls back naturally once the tab's session clears. No child data goes in the URL.*
55. **Marketing opt-in is a separate, unchecked checkbox; the honeypot is the only spam guard this phase.** *Why: reaffirms decision #27 (mandatory consent ≠ optional marketing); full abuse-hardening (rate limiting) is launch-era work already deferred to 2.04/2.07.*
56. **No new dependency for the checkbox.** The new `Input`/`Checkbox` UI primitives are built on the already-installed unified `radix-ui` package (which re-exports `Checkbox`), not a separate `@radix-ui/react-checkbox`. *Why: the brief anticipated adding `@radix-ui/react-checkbox`, but the rest of the UI layer already imports primitives from the unified `radix-ui` package — reusing it keeps the dependency surface unchanged (zero new deps).*
57. **`EmailGate` is code-split via `next/dynamic`, and the temporary `CompletionView` was removed.** *Why: the gate is only reached after the whole test, so loading its JS at the `gate` phase (not in the initial `/test` bundle) is a measured mobile-perf win; `CompletionView` was always a 1.07 placeholder superseded by the gate, so it (and its now-orphaned `Test.completion` copy) was pruned — live code wins.*

### 2026-06-13 — Phase 1.10 results profile + shareable certificate (Code)

58. **Task A — the Phase 1.09 design was delivered as three live HTML mockups, not a written handover.** The mockups (`Result.html`, `Certificate.html`, `Phase-09-Mockups.html`) were filed to `docs/design-handovers/Part-1-Phase-09-assets/`; a companion `tokens.css` they referenced was reconstructed (review aid); and `Part-1-Phase-09-Handover.md` was created as an **honest index** that points at the mockups and transcribes only what they literally contain — it invents no design decisions. The written **1.09 completion report was absent** and could not be located anywhere under the project folder — flagged for Lazar (no fake report created). *Why: file at the canonical paths and make the mockups reviewable without fabricating a handover (Task A rule).*
59. **Copy is split: substantive result copy (spec §6) lives in `src/content/results/`; on-screen chrome (mockup tier labels, buttons, certificate face) lives in the `Result` i18n namespace.** The profile renders both — the constellation is the visual tier summary and a parent "what we noticed" note carries the §6 headline + celebrated blurbs + also/growing lines. The §6A per-strength *growing* fragments are kept in the data (used as the grow-chip `title`; available to the 2.01 results email) while the visible growing line uses the §6B wrapper. *Why: the handover wins on visual layout, §6 is the source copy — surfacing both honours each, and the §6 prose ("the result speaks to the parent") would otherwise not render at all in the mockup's tier-only layout.*
60. **Reveal hero = §6 kid-celebration as the eyebrow + the mockup's "Brilliant work, {name}!" title (chrome) + the mockup's no-scores lede.** *Why: surfaces the §6 "plays-first" kid celebration prominently while keeping the personalised title and the on-brand honesty cue, with no redundant repetition.*
61. **`ResultPlaceholder` was renamed/retired to `ResultView`** (the placeholder file was deleted; `result/page.tsx` updated), and the `Result` i18n namespace was **rewritten** — the temporary `badge/heading/intro/topStrengthsLabel/note/home` keys were retired and `messages.test.ts`'s required-key list updated to the real chrome keys. *Why: the placeholder is fully replaced; keeping its keys/strings would be dead copy. (No silent rename — recorded here and in the report.)*
62. **Certificate generation = `html-to-image` (pinned 1.11.13); the `modern-screenshot` fallback was NOT needed.** The certificate is a real 1080×1350 DOM component captured client-side via `toBlob` (`width/height 1080×1350, pixelRatio 1` → exact Instagram-portrait output) after `await document.fonts.ready`. Verified live: a valid ≈223 KB 1080×1350 PNG with Cyrillic fonts embedded (no tofu). *Why: WYSIWYG, keeps the child's name in the browser (no server/URL), and html-to-image handled the Cyrillic fonts + CSS-var theming + inline SVG correctly in this Next 16 / React 19 stack, so no fallback was warranted.*
63. **Per-child tint = frame `top1-tint → top2-tint`, name flourish = `top1` solid, on a constant cream (`#FFFBF2`) background.** AA therefore holds for every tint the rule can produce: all body text sits on the constant cream and the only text-over-tint is the celebrated chips (`--strength-*-ink` on `--strength-*-tint`, all ≥ 5.6:1). Verified across all six strength ink/tint pairs in `certificate-model.test.ts`. The decorative medallion glyphs (white-on-solid / solid-on-tint) are treated as exempt from WCAG 1.4.11 because the strength **name** is always present beside them as AA-compliant text. *Why: makes the deterministic tint AA-safe by construction rather than per-combination.*
64. **The `spatial` short badge descriptor was reworded from the mockup's "Thinks in 3D" to "Pictures shapes in space".** *Why: "3D" contains a digit; the no-number guardrail (and the forbidden-token test) bars digits in user-facing result copy.*
65. **The result UI was built solo (single context) rather than fanned out to parallel subagents mid-build; a fresh-context review subagent ran at the end (found zero blockers).** *Why: the components share the 1.03 tokens, the strength glyphs, the result-copy contract, and the `/result` page — CLAUDE.md says not to parallelize shared-state tasks; the genuinely-independent unit (the adversarial review) was the part delegated.*
66. **The certificate "render smoke" coverage is pure-model unit tests (tint/sizing/date/AA/strength-list) rather than a jsdom DOM render.** *Why: the repo's Vitest is a node-env logic-test setup; the actual render was verified live (the 1080×1350 PNG) instead of pulling in a jsdom dependency for one smoke test.*
67. **The trial CTA is a graceful working contact behind a `// TODO(booking 2.05)` seam:** a city picker (native `<select>`, no geolocation) reveals the chosen centre and `tel:`/`mailto:` actions + the IqUp contact-form link; the §6 `{center}` slot is filled from the picked city. *Why: real booking is Phase 2.05, but the CTA must still *do* something honest now.*
68. **`src/content/centers.ts` is the single source for the 10 centres (seeded from brand.md §4) and is flagged PROVISIONAL** (phones/addresses vary across sources per brand.md; some carry a `verify` note); a reserved empty `mapsUrl` field was added for 2.05. *Why: single-sourcing for 1.10 + 2.05, with the data-accuracy caveat surfaced for IqUp verification before launch.*

### 2026-06-15 — Phase 1.11 parity + a11y + performance finalisation (Code)

69. **Added three dev-only QA tools, pinned exact:** `@lhci/cli` 0.15.1, `@playwright/test` 1.61.0, `@axe-core/playwright` 4.11.3 (Chromium only). None ship in the app bundle; the Playwright suite runs via its own scripts so `npm test` stays Vitest-only. *Why: turn the per-screen, eyeballed a11y/perf checks of 1.06–1.10 into repeatable, tool-driven, whole-site evidence (median Lighthouse, axe, device screenshots).*
70. **Audits were run in the main session (perf isolated first, then the Playwright suite), with the four `.claude/agents/*.md` auditor definitions committed for re-run — rather than fanning four server-driving subagents out in parallel.** *Why: on one Windows machine, four concurrent `next start` + Chromium instances collide on port 3000 and, worse, pollute the noise-sensitive Lighthouse median; serial-isolated is the honest way to measure here. The agent defs satisfy the "repeatable, version-controlled, scoped" intent.*
71. **Lighthouse is measured with `scripts/lh-median.mjs` on this machine; the `@lhci/cli` configs are kept as the clean-infra/CI artifact.** Every Lighthouse child process here dies on an `EPERM` removing its temp profile dir *after* the audit, which makes LHCI discard the otherwise-valid run. The script calls the Lighthouse CLI directly and reads each report regardless of that cleanup error, then reports the per-URL median. *Why: get a defensible median on Windows without abandoning the canonical LHCI config.*
72. **Lighthouse runs pin `Accept-Language: mk`** so the prefix-less default-locale landing (`/`) resolves to the Macedonian page directly. *Why: next-intl locale-detection redirects Lighthouse's English UA from `/` → `/en`, which fails the run; explicit `/en` still measures English.*
73. **The language toggle preserves the full path + query via `useSyncExternalStore` reading `window.location.search` — NOT `useSearchParams()`.** *Why: `useSearchParams()` opts the static landing/result pages into client-side rendering (a perf regression); reading the browser query post-mount keeps every page static, avoids a hydration mismatch (empty server snapshot), and avoids a setState-in-effect lint error. Fixes the `?age` drop (parity + WCAG 3.3.7).*
74. **Added `scroll-padding-top: 5rem` on `html` (globals.css).** *Why: WCAG 2.2 §2.4.11 Focus Not Obscured — keeps a tab-focused control clear of the 64px sticky `SiteHeader`.*
75. **`html-to-image` is now a dynamic `import()` inside `CertificateCard.renderBlob`.** *Why: it is only needed to export the certificate, never to view the result — dynamic-importing it removes it from the initial `/result` client bundle (§4 perf lever).*
76. **The test `ProgressHeader` progressbar now has an accessible name** (`aria-labelledby` → the visible "Question X of Y" line). *Why: axe `aria-progressbar-name` (serious) — a `role="progressbar"` needs a name, not only `aria-valuetext`.*
77. **404 handling was rebuilt as three files:** a localized `[locale]/not-found.tsx` (skip-link + header/footer, inside the locale layout → correct `<html lang>`), a catch-all `[locale]/[...rest]/page.tsx` calling `notFound()` (so unmatched locale routes hit the localized 404, not the global one), and a self-contained global `not-found.tsx` for Next's internal `/_not-found`. Added a `NotFound` i18n namespace (both locales). *Why: the first attempt (global `not-found.tsx` only) caused a hydration mismatch on locale-prefixed unknown URLs because its own `<html>` conflicted with the `[locale]` layout; this is the next-intl-correct structure. The "404" uses brand blue (`text-secondary`) not the hero yellow — the yellow-on-cream failed axe contrast.*
78. **Dev-only chrome (the `DevBar` and the gate's dev strengths panel) is tagged `data-dev-only` and excluded from axe scans.** The 8 "gate" colour-contrast violations axe first reported were entirely this dev-only UI (reached via `?dev=1`), which is stripped in production — not the real gate. *Why: scan the production gate faithfully while still using the dev shortcut to reach the gated state without a live Supabase write.*
79. **The axe + screenshot suite runs against the dev server; Lighthouse runs against the production build.** *Why: axe findings are DOM/ARIA-equivalent dev↔prod, and the dev server lets the gate be reached via `?dev=1` (no DB write) and injects the `/result` sessionStorage hand-off cleanly; Lighthouse needs the prod build for accurate perf. The Next dev overlay is excluded from axe and removed before screenshots, so neither reflects dev-only UI.*
80. **Workstream E:** deleted the two non-canonical content-spec copies (`Part-1-Phase-04-Content-Spec.md` root + `docs/Part-1-Phase-04-Content-Spec.md`), keeping the canonical `docs/content/` copy; the only references to the deleted paths are inside **filed historical completion reports (04/07/10), which were left unedited** (rewriting filed history would be dishonest); no code referenced them. Corrected `.mcp.json`'s Supabase `project_ref` to the canonical EU leads project `cpxssfodboukznzaksnb` — and **committed it: `.mcp.json` is in fact tracked in the repo, so the phase prompt's "untracked" was a mismatch with the live code (live code wins); committing the fix is the only way the repo stops carrying the wrong project ref.** *Why: tidy the 1.10-flagged duplicates and point a future local Supabase-MCP session at the right project, without falsifying past reports.*
81. **`/result` states are reached in tests by injecting the real `iqup.testResult.v1` + `iqup.leadContext.v1` sessionStorage hand-off** (valid `TestResult`/`LeadContext` fixtures per band). *Why: render every band's result/certificate for axe + screenshots without driving a live lead submit (no Supabase row, child name stays in the browser) — the same contract the runner persists.*

### 2026-06-15 — Phase 2.01 results email (Code)

82. **The email reproduces the on-screen ranking from the lead's stored ratio summary (`top_strengths.scores`) via a new `reconstructResult`, rather than persisting anything new.** `score.ts` was refactored to export the shared `compareStrengthScores` + `tierForRank`, which both `score()` (from answers) and `reconstructResult()` (from the rounded ratio map) use — so the emailed result is the byte-identical ranking the screen showed. A cross-check test proves the 2-dp rounding the lead stores preserves the ranking for every band × answer shape. *Why: honours the "no new data in the lead payload" guardrail (decisions #25/#51) while guaranteeing the email content equals the on-screen content.*
83. **Email chrome lives in a new `Email` i18n namespace (chrome only); the strengths body copy comes from `getResultCopy` (the single source) and is never duplicated.** The chrome deliberately avoids even *negated* forbidden words (no "scores"/"numbers"/"оценки"/"бројки") so the extended forbidden-token test stays a clean tripwire over the whole email. All new MK is provisional (native-MK review); the footer identity line is flagged for IqUp legal, tied to `CONSENT_VERSION`. *Why: single-source the copy; keep the no-number guardrail strict and testable.*
84. **The email certificate is a NEW Satori renderer (`next/og`), not a reuse of the DOM `Certificate.tsx`.** It reuses the data + deterministic tint logic but re-expresses the layout in Satori's CSS subset: literal brand hex from `src/lib/email/brand.ts` (no CSS vars / `color-mix`), flexbox only (no grid), soft mixes precomputed, the `✦` decoration swapped for an inline SVG sparkle (the brand fonts lack U+2726 → it would otherwise render as tofu), the radial vignette swapped for the constant cream, and SVG children passed as keyed arrays (the bundled Satori crashes on a Fragment as an SVG's only child). The per-child tint is derived internally from the celebrated codes (Satori needs literal hex), faithful to decision #63. Fonts: Rubik + Nunito Sans, Latin **and** Cyrillic, embedded from `@fontsource` (the proven OG pattern) — Cyrillic names render with no tofu (verified by rendering a real sample PNG). Bibi art via the existing `BIBI_CERT_ART` placeholder — never generated. *Why: Satori is not a browser; faithful re-expression within its limits, with the licensed-art + no-number rules intact.*
85. **The certificate renderer reads its localized face labels straight from the `Result.certificate` messages JSON, and the email recipient `to` carries only `{email}` (no display name).** *Why: keeps `renderCertificatePng({childFirstName, celebrated, locale, date})` self-contained and matched to the on-screen face; the recipient is the parent and we don't collect a parent name (GDPR minimisation), so a child-name display name would be wrong.*
86. **Build split / parallelisation:** the two large independent tracks — the React Email template (`src/emails/`) and the Satori certificate (`src/lib/email/certificate-image.tsx`) — were built by parallel subagents against typed contracts (`src/emails/types.ts`, `src/lib/email/brand.ts`). The integration spine (Brevo client + orchestrator + the `submit-lead` `after()` hook) and the foundation (deps, shared types, brand hex, `reconstructResult`, the `Email` namespace) were built in the main session, since they import both tracks and wire the action. A fresh-context review subagent re-verified the §5 guardrails at the end. *Why: parallelise the genuinely-independent work; keep the integration where the full context lives (consistent with decision #65).*
87. **`npm run test:email` runs under a script-local tsconfig that aliases `server-only`→empty, NOT `--conditions=react-server`.** That condition (used by `test:insert`) blocks `react-dom/server`, which `@react-email/render` needs; the alias neutralises the `server-only` tripwire while letting `react-dom/server` + `next/og` run — exactly as they do in the real Next server runtime, **verified live** (a temporary route handler rendered real HTML + a valid 1080×1350 PNG in the running dev server). *Why: the dev script needs the same render capabilities the production send path has.*
88. **Brevo is a NEW data processor — surfaced, not silently ratified.** This phase sends the child's first name + parent email + strengths summary to Brevo (a third party). Recorded here and in the completion report, and **added to the Part-2 legal-review list: Brevo DPA + EU data residency** (alongside the existing consent/privacy sign-off). The dev sender comes from Cowork's `.env.local`; the branded `@iqup.mk` sender + SPF/DKIM/DMARC are 2.06. *Why: the no-silent-ratification rule — a new external recipient of children's/parent PII must be flagged for IqUp legal before launch.*

### 2026-06-16 — Phase 2.02 CRM contact routing + new-lead notification (Code)

89. **The 2.01 `after()` hook became a single fan-out orchestrator** (`src/lib/leads/after-lead.ts` → `runAfterLead`) that runs THREE isolated side-effects — results email (2.01), Brevo contact upsert (2.02), internal notification (2.02) — each wrapped in an `isolate` try/catch inside `Promise.allSettled`; `submitLead` now schedules `after(() => runAfterLead(...))` and captures `insertLead()`'s returned `created_at` as the lead timestamp. *Why: extend (not duplicate) the 2.01 hook; one isolation layer guarantees a side-effect that fails — even synchronously, before returning its promise — can never block its siblings, the lead save, or the `/result` redirect; the DB `created_at` is an authoritative save timestamp that requires storing nothing new.*
90. **The Brevo Contacts client is a thin typed `fetch` mirror of `brevo.ts` (no SDK):** `POST /v3/contacts` with the `api-key` header and `updateEnabled: true` (upsert by email). It tolerates a `204 No Content` (Brevo's "updated" response) without crashing on an empty body, and `201 {id}` on create. **The returned contact id is discarded — never persisted; Supabase stays the single system of record (stateless).** *Why: reuse the proven 2.01 thin-client pattern; honour the baked-in stateless-upsert decision (no new column / no Brevo id stored).*
91. **The contact carries exactly eight UPPERCASE attributes, with a digit-free human BAND label:** `CHILD_FIRST_NAME` (text), `CHILD_AGE` (number), `BAND` (text — `bandLabelFor` → e.g. "Ages six to nine", never the internal `band-a/b/c` code), `LOCALE` (text), `MARKETING_OPT_IN` (boolean), `CONSENT_VERSION` (text), `TOP_STRENGTHS` (text — the two **celebrated** strength names), `SOURCE` (text = `website-quiz`). *Why: a human-readable, segmentable attribute set; Cowork must create these exact names + types in Brevo (Contacts → Settings → Contact attributes) for the values to land — documented for §7.*
92. **`TOP_STRENGTHS` uses ENGLISH strength display names regardless of the parent's locale** (the `LOCALE` attribute records the parent's language separately). *Why: the contact attribute is an operational/CRM field consumed by the IqUp team and 2.03 segmentation — one consistent vocabulary is cleaner than mixing locales in a column; the parent-facing localized names already live in the results email + screen.*
93. **The internal new-lead notification is ENGLISH-only (not bilingual).** *Why: it is an internal ops alert to the IqUp team (the data controller acting to follow up its own lead), not parent-facing copy — the bilingual mk/en rule governs parent-facing strings; the parent's chosen locale is reported as a labelled field instead.*
94. **No-number guardrail strategy, split by surface.** The **notification body** keeps the strict no-digit tripwire: the child age is rendered as a WORDED label (`ageInWords` → "seven"), and the test masks the three unavoidable digit-bearing values — parent email, consent version, timestamp — before the digit ban (exactly as 2.01 masked URLs/emails). The **contact attributes** get the forbidden-WORD guard (score/IQ/rank/% vocabulary) but NOT a blanket digit ban, because `CHILD_AGE` (a number) and `CONSENT_VERSION` (e.g. `v1-draft-2026-06`) legitimately contain digits. *Why: §5 bans the score vocabulary everywhere and stray score-digits in visible copy; digits are legitimate structured data in CRM attributes; this keeps one clean visible-text tripwire (Task C's stated preference) without false positives on the age/consent/date.*
95. **Safe list-id parsing:** an unset/blank `BREVO_LEADS_LIST_ID`/`BREVO_MARKETING_LIST_ID` → `null` silently (intentional config, filled in at §7); a present-but-invalid value → `null` plus a `list-config-invalid` log; the upsert RUNS whenever `BREVO_API_KEY` is set, for whichever lists parsed. *Why: Task E — missing/NaN list → skip with a log, while the contact still upserts with its attributes; avoids log noise on the expected local no-config path.*
96. **The operational-list-vs-marketing-list consent boundary is escalated to IqUp's legal/privacy reviewer; no NEW processor is added** (Brevo was already placed on the Part-2 legal-review list in #88). *Why: the no-silent-ratification rule — the single most important guardrail this phase (marketing-list membership ⇔ `marketing_opt_in === true`) is a consent decision the reviewer must see; 2.03's nurture sequences run on the marketing list this phase populates.*

### 2026-06-16 — Phase 2.03 follow-up nurture emails (Code half)

97. **The trial-CTA target is a shared seam: `siteUrlFor` was extracted from `send-results-email.ts` (2.01) into a new pure `src/lib/email/site-url.ts`, and 2.01 now imports it** — so the 2.01 results email and the four 2.03 nurture emails resolve their trial-CTA target from ONE place. Pure (no `server-only`) so the nurture templates (rendered under Vitest + the render script) import it without a mock. Behaviour is byte-identical — the 2.01 `send-results-email.test.ts` still passes (incl. the `https://iqup.example/en` locale-prefix assertion). *Why: the brief requires the nurture trial CTA to be identical to the 2.01 one "so 2.05 updates both in one place" — a single shared seam is the honest way to guarantee that, and it's a no-behaviour-change refactor.*
98. **Merge tags keep Brevo's documented `default: "…"` syntax, and the render helper restores the literal quotes React escapes inside `{{ }}` spans (`finalizeMergeTags`).** Empirically, `@react-email/render` HTML-escapes `"`→`&quot;` in text content (verified with a throwaway probe), which would otherwise ship `default: &quot;…&quot;` and break Brevo's filter parsing. `finalizeMergeTags` un-escapes quotes ONLY inside `{{ … }}` (confined, so no legitimate copy is touched); the `{{ unsubscribe }}` href has no quotes and is unaffected. The render smoke asserts the literal-quote tag is present and no `&quot;` remains inside a tag. *Why: the brief's "render verbatim into the HTML" is only true for the braces, not the quotes; the helper makes it true for the whole tag, in one tested place.*
99. **The empty-name fallback is a meaningful default (`"your child"` / `"вашето дете"`), not the brief's `default: ""` example; the greeting is generic (`Hello,` / `Здраво,`).** A meaningful default reads naturally in every sentence ("…with your child."), whereas an empty default leaves dangling punctuation; the greeting is parent-facing and we collect no parent name (GDPR minimisation, #85), so the child-name merge tag lives in the body, not the greeting. *Why: the brief's stated intent ("a greeting that reads naturally with or without a name") is best met by a meaningful default; `default: ""` was an example, not a mandate.*
100. **`welcome-general` carries a quiet "general" link (its own `nurture-welcome-general` UTM campaign), not a trial CTA; the three trial emails share ONE identical trial-CTA label (matching the 2.01 email).** This keeps "every outbound link is UTM-tagged" + "the four campaigns appear on the right emails" true while "no trial CTA in welcome-general" holds, and makes the trial-CTA presence/absence a clean string tripwire. *Why: welcome-general still needs a UTM-tagged link for 2.04 attribution, but must not push the trial; an identical trial label across the trial emails is both on-brand and testable.*
101. **The legal entity stays Latin `IKUP d.o.o.` in both locales (only the street/city is localized), and the footer identity tagline + signoff are REUSED from the 2.01 `Email.footer` messages.** The registered entity name (brand.md §1) and the brand itself render in Latin throughout the MK site, so a Latin entity name is correct and gives a stable, assertable legal token; reusing the 2.01 tagline/signoff keeps the email family single-sourced. The legal/postal line + marketing wording is flagged for IqUp legal, tied to `CONSENT_VERSION` (continuing #83/#88/#96). *Why: legal correctness + a stable token + no duplicated brand strings.*
102. **No-number guardrail split by surface (continuing #94): the message copy gets the full digit + score-word ban; the footer LEGAL/postal line gets the forbidden-WORD ban only** (a postal address legitimately has digits — No. 4, 1000). In the render-smoke the legal line + URLs + the brand wordmark `IQ UP!` (whose standalone "IQ" is the brand name, not a score) are masked before the digit ban. The forbidden-token test is proven non-vacuous (the fresh-context review injected `you scored 7` and both layers went red). *Why: §5 bans score-digits in visible copy, but a postal address and the brand name are legitimate; this keeps one clean tripwire over the real prose without false positives.*
103. **Built in the main session (not fanned out), with the render smoke under the DEFAULT Vitest env.** The four templates share the layout, brand, footer, and copy contract (shared-state main-session work per CLAUDE.md, consistent with #65/#86); the genuinely-independent unit — the end-to-end fresh-context guardrail review — was the part delegated to a subagent. The render smoke needs no script-local tsconfig because the nurture templates import no `server-only` (exactly like 2.01's `ResultsEmail.test.ts`, already green under `npm test`). *Why: parallelise only what's genuinely independent; keep the render guardrail in the standard `npm test`.*

### 2026-06-19 — Dev machine switch: Windows → macOS

104. **The canonical local project root moved from Windows `C:\Users\user\Desktop\iqup-web` to macOS `~/Projects/iqup-web`** (`/Users/petarjakimov/Projects/iqup-web`). Lazar switched to a new Mac; the repo was re-cloned fresh from `DinovLazar/iqup-web` (branch `main`, clean tree). The path reference was updated in `project-instructions.md` (3 places) and `CLAUDE.md` (1 place); `AGENTS.md` carried no local-path reference, so it needed no change. Toolchain on the Mac: git 2.50.1 (Apple), Homebrew 6.0.2 (pre-existing — only wired onto PATH, not reinstalled), gh 2.95.0, Node v24.17.0 LTS via fnm, npm 11.13.0; deps installed with `npm ci`; typecheck + production build both pass. `.env.local` was recreated from `.env.local.example` (gitignored, all secret values empty — none fabricated); the live Brevo/Supabase values still need filling in. *Why: record the machine move so the canonical local path in the docs matches reality, and log the one off-spec env choice (next item).*
105. **In the new `.env.local`, `NEXT_PUBLIC_SITE_URL` is left commented-out (genuinely unset), not present-with-an-empty-value.** Copying `.env.local.example` verbatim set it to an empty string, which broke `next build` (`new URL('')` → `ERR_INVALID_URL` at prerender) because `src/app/[locale]/layout.tsx` and `src/lib/email/site-url.ts` use `?? 'http://localhost:3000'` — a nullish fallback that catches `undefined` but NOT `''`. Commenting the line restores the documented "unset → falls back to localhost:3000" dev behaviour. No value was invented (it is a public URL, not a secret). *Why: the example template's trailing-`=` lines assume `??`-friendly unset semantics; an empty string defeats the fallback. This only affects the local gitignored `.env.local`; the example template and product code were left untouched.*

### 2026-06-19 — New-machine checkpoint: verify build green on macOS + reconcile project-state docs

106. **The post-move verification + doc-reconciliation checkpoint was run as a read-only audit + docs-only edit pass** (its own commit + `Part-2-Checkpoint-Verify-Reconcile-Completion.md`), separate from the #104/#105 machine-move commit. Build health on macOS recorded **verbatim**: `npm run test` → **258/258 passing** (24 files); `npm run typecheck` clean; `npm run lint` clean; `npm run build` green (13/13 pages). MK/EN i18n parity is **perfect** (139 keys each, both directions). State docs reconciled to the live repo: `phase-plan.md` Status set to `[x]` for 1.01–1.11 + 2.01–2.03 (every prior phase showed `[ ]`); `current-state.md` "Last updated" refreshed to 2026-06-19 with the move + checkpoint noted and the Windows-only LHCI `EPERM` note de-Windows-ified; `file-map.md` got the three completion reports it was missing (1.03, 1.04, 1.05-Cowork) plus the new checkpoint + Mac-Setup reports. The Phase 1.03 design handover was **confirmed already filed** at `docs/design-handovers/Part-1-Phase-03-Handover.md` (no move needed). No product copy, test questions, scoring, results, design tokens, Bibi assets, or Brevo/Supabase logic were touched. *Why: prove the build is healthy on the new machine and make the project's own records true again, without touching product behaviour — the lowest-risk re-entry into the build.*

107. **FINDING (flaky a11y test, NOT a shipped defect — left for Lazar, not fixed in this pass): the `@axe-core/playwright` WCAG 2.2 AA suite is timing-flaky on the faster macOS machine.** 10 of 26 scans (the "test first question" + all "result band" states, both viewports) reported a *serious* `color-contrast` violation. Root cause traced conclusively: those states wrap content in a `tw-animate-css` `animate-in fade-in-0 … duration-300` **entrance fade**, and `scan()` analyses immediately after the element appears — i.e. **mid-fade, ~opacity 0.36** — so axe measures the strength-chip tokens (`--strength-*-ink` on `--strength-*-tint`, a strong ~9:1 in the settled UI) *composited toward the page background* (e.g. ink `#3a33ae`→`#b8b4db` over tint `#ecebfb`→`#f6f3f6` = 1.8:1). Proven two ways: (a) the observed faded colours are exactly the tokens at ~36% alpha; (b) **adding a 700 ms settle-wait before the scan → all 26 pass** (verified, then reverted — this pass does not modify tests). `prefers-reduced-motion` does NOT fix it (the fade is a CSS `animate-in` utility, not a `useReducedMotion`-gated Framer Motion animation — verified). The shipped UI passes WCAG 2.2 AA; this is a test-determinism gap that surfaced only because the new machine renders fast enough to scan before the fade completes (the old Windows machine was slow enough to miss it). **Recommended fix (Lazar's call — a real test change, out of scope here): make `scan()` wait for entrance animations to settle before analysing** (e.g. await `getAnimations()` draining, an `animationend`, or a short `waitForTimeout`) — strengthens determinism without weakening coverage. *Why surfaced not fixed: the guardrails bar weakening/altering tests in this pass and bar changing design tokens; the honest move is a documented finding with root cause + recommended fix, not a silent edit.*

### 2026-06-19 — Phase 2.04 analytics, consent layer + `/privacy` (Code)

> Decision numbering continues from the live file (#107), not the phase prompt's "#104" — the macOS-move checkpoint already appended #104–#107. (Live code wins; mismatch noted in the completion report.)

108. **The cookie/tracking consent is a brand-new, fully separate consent from the lead's parental consent.** New constant `COOKIE_CONSENT_VERSION = 'cookies-v1-2026-06'` + first-party cookie `iqup_consent` (`src/lib/consent/`), with **zero coupling** to the lead's `CONSENT_VERSION` / `leads.consent` (Phase 1.08). The two never share state, names, versions, or imports. *Why: guardrail §5.4 — conflating "consent to process the lead" with "consent to set tracking cookies" would be both legally wrong and a data-protection risk; keeping them independent keeps each auditable.*

109. **Deny-by-default with "don't load the script at all until consent"** (not Google/Meta's "load denied + cookieless pings"). On every load the `iqup_consent` cookie is read; GA4 + Clarity inject **only** on Analytics grant, Meta Pixel **only** on Marketing grant; each is also env-gated (no `NEXT_PUBLIC_*` id → never loads, logged no-op). GA sets Consent Mode defaults-denied then immediately updates to actual state; Clarity uses `consentv2`; Pixel does `consent grant` → init → PageView. Withdrawal re-signals denied/`revoke` and stops `track()`; an already-injected third-party script is **not** force-unloaded mid-session (a fully clean state lands on the next navigation — documented honestly in the Manage-panel note + the privacy page). *Why: §2.2 — for a children's-data project data minimisation comes first; the strictest defensible model.*

110. **The consent provider is backed by `useSyncExternalStore` over the cookie, not a setState-in-effect.** The cookie is the single source of truth; reads go through a cached external store with a stable `NOT_READY` server snapshot (banner renders post-hydration → no mismatch / CLS), and every write re-reads + notifies. *Why: the repo's ESLint forbids setState-in-an-effect (the `LanguageToggle`/`ResultView` idiom, Decision #73); this keeps the static pages static and avoids a hydration mismatch.*

111. **`track()` is a single PII-free client helper with per-category routing.** It sanitises params to **exactly** `{band, locale, path}` (dropping any other key — unit-asserted), fires GA iff Analytics granted + GA id set + `gtag` present, and Pixel iff Marketing granted + Pixel id set + `fbq` present, independently. The four seams: `test_start`/`test_complete` (`TestRunner`), `generate_lead`+Pixel `Lead` (`EmailGate`, **client-side after `submitLead` returns success — never inside the server action**), `trial_cta_click` (`TrialInvite` tel/mailto/contact-form). Page views fire on client navigation via `usePathname()` (never `useSearchParams()`, Decision #73), skipping the first path (the SDKs send the initial page view). *Why: §2.4 — one auditable choke point; no child name/email/age/answers can ever reach an analytics event.*

112. **The banner + Manage dialog are `next/dynamic` (ssr:false) code-split out of every page's initial bundle.** They are client-only + render post-hydration anyway, so deferring them (esp. Radix Dialog) keeps hydration — and the throttled mobile LCP — light on the JS-heaviest route (`/test`). *Why: §5.7 perf — the consent island is mounted in the locale layout (every page); shipping Radix Dialog eagerly to landing/result/test for a banner that paints after mount is wasteful.*

113. **Accept and Reject are rendered with IDENTICAL styling (same fill, contrast, elevation, size, weight), not just the same size.** Both are solid `secondary` buttons with the same shadow; Manage is a clearly lower-emphasis outline button. *Why: §5.2 + EDPB dark-pattern guidance — "reject as easy as accept" includes equal *salience*; the first draft gave Accept the hero-gold + hero-shadow (more eye-catching). The fresh-context review flagged it; equal styling removes any nudge. An e2e assertion now compares the two buttons' computed background/shadow/weight/height.*

114. **The `/privacy` page is a provisional GDPR baseline (`privacy-v1-draft-2026-06`), IqUp-legal-pending, all-MK-provisional, and exempt from the no-number rule** (it legitimately carries dates, durations, the address) — but it introduces **no** score/IQ vocabulary (enforced by a forbidden-vocab test that is careful not to false-positive on the `IqUp` brand name). Controller = IKUP d.o.o.; the privacy contact is a flagged provisional `info@iqup.mk` (`// TODO(IqUp: confirm privacy contact email / DPO)`); the MK slug stays `/privacy` for now (`// TODO(mk-slug)`). GA4 / Clarity / Meta Pixel are added to the standing Part-2 legal-review list as data processors (continuing #88/#96). *Why: §4/§5.9 — we are not lawyers; the policy is shipped working but explicitly flagged for sign-off, tied to a visible version + last-updated date.*

### 2026-06-19 — Phase 2.05 trial-booking mechanic (Code)

115. **The trial booking is a direct-contact mechanic, not a form or scheduler (pre-resolved in chat, built exactly).** A parent picks their city and gets one-tap ways to reach their nearest IqUp centre — **call**, a **name-free pre-filled email**, **get directions**, and (only where present) **Viber / WhatsApp** — plus a link from every trial email. *Why: it matches how IqUp actually books trials today (per-centre, by phone/email — there is no public calendar), collects **no new personal data**, adds **no new third-party processor or dependency**, and is built so a future scheduling tool changes only the action targets. The lead is already captured at the gate (1.08) and IqUp already gets an internal alert per submit (2.02); this only gives a keen parent an immediate, structured way to act.*

116. **One shared `TrialBooking` client component is the single home for the picker + actions, reused by BOTH surfaces** (`src/components/trial/TrialBooking.tsx`): the public `/trial` page and the result screen's `TrialInvite` render the exact same mechanism, so there is no second copy. The component owns the native `<select>` (no geolocation — manual choice, privacy-preserving), the chosen-centre card, and the Call/Email/Directions/optional-Viber-WhatsApp action row + their labels; each **surface** supplies its own surrounding heading/intro. Copy is resolved server-side from the new `Trial` namespace via a shared `resolveTrialBookingCopy(locale)` and passed as plain-string props, so the island ships no translation runtime (the 1.06/1.08/1.10 idiom). *Why: guardrail "one mechanism, no duplication" + the established "don't fan out shared-state work / pass copy to islands" pattern (#65/#86/#103).*

117. **The §6 `{center}` slot on the result screen is preserved via an `onSelectCenter` callback, not by moving the heading/intro into the shared component.** `TrialBooking` notifies its surface of the chosen city label; `TrialInvite` holds that in state and fills `{center}` (default "your nearest center" before a pick). *Why: keeps the shared component surface-agnostic (the public page passes no callback and has no `{center}` slot) while keeping the result screen's dynamic intro behaviour byte-identical to 1.10.*

118. **`centers.ts` stays the single source — only additive, no value changes.** Added optional `viber?`/`whatsapp?` (E.164) left **unset** for every centre (not fabricated); `mapsUrlFor(center)` uses a verified `mapsUrl` when present and otherwise **derives an honest Google Maps *search* link** from the published city+address (`https://www.google.com/maps/search/?api=1&query=…`); `viberHref`/`whatsappHref` build `viber://chat?number=…` / `https://wa.me/<digits>` only when the number exists (else the button is simply not rendered). The PROVISIONAL flag + every existing phone/email/address are unchanged. *Why: guardrail — don't fabricate contact data or claim exact pins; degrade gracefully until Cowork supplies real values.*

119. **One shared `trialBookingUrl(locale, utmCampaign?)` helper (`src/lib/email/site-url.ts`) is now the single trial target for every email + the on-screen surface.** Built on `siteUrlFor` (so the `NEXT_PUBLIC_SITE_URL`-unset → `http://localhost:3000` fallback from #105 is preserved) + the `/trial` slug + optional UTM. The results email (`send-results-email.ts`, bands 3–5/6–9) and the three trial nurture links (`welcome-trial`/`trial-invite`/`nudge`) all resolve from it; UTM is preserved on the nurture links (results email keeps **no** UTM — it had none in 2.01). **`welcome-general` keeps its existing general "explore IqUp" site-root link — NOT a trial CTA** (#100). *Why: guardrail "one trial target" — the result screen, the results email, and the three nurture emails update in exactly one place.*

120. **The results-email `ResultsEmailProps.siteUrl` prop was renamed to `trialUrl` (intentional, non-silent).** It now holds the booking-page URL, not the locale root, so the name matches the value. This drove two **deliberate** test updates: `send-results-email.test.ts` (`https://iqup.example/en` → `https://iqup.example/en/trial`, mk root → `/trial`) and `ResultsEmail.test.ts` (the `SITE_URL` constant + button-href assertion → `/trial`). *Why: clarity over a misleadingly-named `siteUrl` that points at `/trial`; flagged here and in the completion report per "no silent changes".*

121. **Removed `IQUP_CONTACT_URL` and the contact-form fallback link (superseded).** The 1.10 temporary mechanic linked to the central IqUp contact form behind the `// TODO(booking 2.05)` seam; the real mechanic replaces it with the structured Call/Email/Directions/Viber-WhatsApp actions, so the contact-form link + its export + its test assertion were removed (no second contact path). *Why: guardrail — remove the now-superseded inline picker/seam so there is no second copy of the mechanism.*

122. **Fixed a latent AA contrast defect surfaced by exercising the chosen-centre card.** The contact line was `text-ink-faint` (#8a8499) on `bg-canvas` (#fbf8f3) = **3.39:1** at 12px — below 4.5:1. It existed in the 1.10 `TrialInvite` card but the `/result` axe scans never selected a city, so the card never rendered; the new `/trial` selected-state axe scan caught it. Changed to `text-ink-soft` (#5a5570, ~6.5:1). *Why: the fix lives in the shared component, so it hardens the result screen too; honest to fix a real defect the new coverage exposed rather than exclude the element.*

123. **`/trial` is a provisional slug (`// TODO(mk-slug)`, mirrors `/privacy`) with a generic, name-free per-locale OG image.** SSG inside the `[locale]` layout, per-locale `generateMetadata` (canonical + hreflang), name-free and collects nothing; the OG image reuses the `/result` Cyrillic-safe `next/og` pattern. *Why: the native-MK reviewer may localise the slug later (e.g. `/проба`); the page must never preview child data, exactly like `/result`.*

### 2026-06-21 — Phase 3.01 repo onto v2 footing (Code)

124. **Pentagon = custom SVG, not a charting library (locked).** The 5-index pentagon is a brand-signature visual that must render identically on screen *and* inside the PDF report, be fully brand-styled, and be accessible. *Why: a custom SVG component does all three with no heavy dependency; revisit only if the Design phase (3.02) surfaces a concrete reason a library is needed.*

125. **PDF generation = `@react-pdf/renderer` (locked; smoke-tested on React 19).** Installed `@react-pdf/renderer@4.5.1` (pinned exact) and ran a throwaway one-page render under the live React 19.2.4 stack → valid `%PDF` buffer (deleted the script after). *Why: server-side, React-based, Vercel-friendly, supports custom fonts + embedded SVG; the React-19 compatibility risk the phase flagged was checked and cleared. The pentagon stays a custom SVG (no charting library — #124).*

126. **Montserrat loaded via `next/font` (Google Fonts), not a separate font dependency.** Cyrillic + Latin subsets, variable font (covers ExtraBold 800 / Bold 700 / SemiBold 600 / Regular-Medium 400–500), exposed as `--font-montserrat` → the `font-brand` Tailwind token. *Why: `next/font` self-hosts + optimises with zero runtime dep; matches the existing Rubik/Nunito loading idiom. Additive — the v1 Rubik/Nunito UI is untouched and migrates onto Montserrat as v2 screens are rebuilt.*

127. **Authored the v2 `plan.md` and `brand.md` §6 from the canonical spec PDF — there was no ready-made v2 markdown to drop in (on-the-fly decision).** The phase brief said "Lazar supplies the two files"; after confirming none existed on disk, the canonical source was located (`~/Documents/IQ UP Specifikacija v1.2 FINAL.pdf`, MK, 37pp) and `plan.md` was rewritten as the v2 master spec + `brand.md` §6 updated DRAFT→confirmed, both faithfully derived from it (no invented spec). `plan.md` now names that PDF as the authoritative source. *Why: Task 1 ("land the v2 canonical docs") could not be met from invented content; the spec is the ground truth and was used verbatim. Flagged for Lazar in the completion report.*

128. **`phase-plan.md` left as v1 (pending).** It still lists the v1 phases (1.01–2.08); no v2 phase-plan file was provided. Not edited this phase per the brief ("replace it too *if* Lazar provides the file — otherwise note it as pending"). *Why: the v2 phase sequence is Chat's to write once this footing lands; inventing it here would overstep.*

129. **Deleted the stray `Part-2-Phase-06-Cowork-HANDOFF.md` and ran `npm audit fix`.** The untracked v1 Vercel-deploy handoff was superseded (v2 deploy is re-planned) → removed per the brief. `@react-pdf/renderer` pulled a transitive `hono` **high** advisory in code paths we never invoke (serve-static/Lambda/CORS); the non-breaking `npm audit fix` cleared it. The remaining 2 production advisories (`postcss`/`next`) are pre-existing and only "fixable" by a breaking Next downgrade — left as-is. *Why: keep the dependency tree honest; don't do breaking dep surgery in a foundation phase.*

### 2026-06-22 — Phase 3.03 engine + scoring (Code)

130. **v2 scoring lives under `src/lib/scoring/v2/`; v1 is untouched.** The v1 strengths scorer (`score.ts`/`reconstruct.ts`/`types.ts`/`storage.ts`/`index.ts`) is still wired into un-rebuilt v1 surfaces, so v2 was built as a separate subtree (`scoring/v2/` + the previously-empty `engine/`, `validity/`, `content/norms/` scaffolds). v2 imports nothing from v1; v1 imports nothing from v2 (verified). *Why: the phase requires v1 green + clear separation; a subpath is the least-ambiguous split and avoids touching the v1 barrel. v1 removal is a future cleanup, once its last consumer is rebuilt.*

131. **PRNG = mulberry32, seeded via an xmur3 string hash, with per-domain `deriveSeed`.** `makeRng(number|string)` (string → xmur3 → uint32); each domain draws from `mulberry32(hash("${seedInt}::${domain}"))` so domain run-order never perturbs another domain's stream. No `Math.random()`/`Date.now()`/`new Date()` anywhere on the engine/scoring/validity path. *Why: mulberry32 is tiny, dependency-free, auditable, and well-distributed for stimulus selection (non-crypto); 3.04's generators reuse the same utility. Documented in `prng.ts`.*

132. **Start-level table = the spec's Gf example, applied to every domain (PROVISIONAL for non-Gf).** Дел 5 tabulates start level by exact age only for Gf (5→1…13→8), labelled "пример, Gf". We apply that curve to all domains as the default, flagged PROVISIONAL in `norms`. Gsm additionally keys its concrete span off Прилог B.1 (3.04's provider maps level→span). *Why: the spec gives one concrete curve; reusing it is the faithful default rather than inventing per-domain curves. Recorded for the psychologist review.*

133. **Domain caps + ceiling: Gf/Gv max 6, others max 5, +1 for the 10–13 extended battery; discontinue at 2 consecutive errors; no hard minimum.** Target 4–6 (Дел 5); single-signal indices Gf/Gv get +1 (spec 3.2); 10–13 runs longer (Дел 5). The ceiling may legitimately end a domain at 2 items (a real floor); that is reported honestly via low confidence rather than forced to a minimum. *Why: faithful to basal/ceiling; an artificial minimum would mask a genuine floor. `min` is kept only as a soft confidence input.*

134. **Gsm format policy: forward-only under 8; alternating forward/backward from 8 (PROVISIONAL alternation).** The engine selects the format per item index (even→forward, odd→backward from age 8); the provider supplies the matching item. *Why: the spec mandates forward+backward from 8 but not the interleave order; alternation measures both within the 4–6 item cap. Flagged PROVISIONAL.*

135. **Normalization families + per-age handling.** Прилог B.2's three formulas implemented verbatim (accuracy `20+acc·75`; span `50+(span−exp)·14`; speed `50+(net−exp)·6`; all `clamp [8,99]`). The **accuracy form is reused for EF/Glr/attention** (their raw is a [0,1] quality; B.2 gives an explicit formula only for the accuracy/span/speed families). Per-age: span+speed carry explicit per-age `expected` terms; the **accuracy family folds age via the adaptive difficulty** (age-keyed start level → harder reached items → difficulty-weighted accuracy), the WISC-style mechanism. *Why: stays inside the spec's given formulas without inventing per-age accuracy tables; documented in `normalize.ts`/`README`.*

136. **PROVISIONAL per-age speed expectation + a calibration model.** The spec gives the Gs formula + the B.2 index formula but NO per-age speed-rate table → `EXPECTED_SPEED_BY_AGE` is a defensible PROVISIONAL table tuned so a typical child ≈ 50. Gs time is made device-independent via `effectiveSeconds = rawSec × (REFERENCE_TAP_BASELINE_MS / calibrationBaselineMs)`, with `calibrationBaselineMs` an input (captured by the first practice task in 3.05). **Known artifact:** because the provisional speed `expected` is small, an all-wrong Gs still normalizes to ~46 (solid); flagged for the psychologist review. *Why: a real per-age speed norm must come from the pilot; the formula + calibration contract are spec-faithful now.*

137. **Span-range midpoints.** Прилог B.1 gives ranges for ages 6/9/12 (4–5, 5–6, 6–7) → encoded as midpoints 4.5/5.5/6.5, flagged PROVISIONAL. *Why: the index formula needs a single number; the midpoint is the neutral choice.*

138. **Confidence model (PROVISIONAL thresholds); an index's confidence = its weakest contributing signal.** Per-signal score from item count (≥5→+2, ≥3→+1) + answer consistency (reversal-based, ≥0.6→+1), then capped by validity (chance-level→low; gentle_note→≤medium; not_representative→low); ≥2 high / 1 medium / else low. Index confidence is the **weakest** of its signals. Attention's "consistency" input is `1 − normVariability` (it has no correctness-reversal sequence). *Why: spec 6.5 gives the method, not numbers → flagged PROVISIONAL for review; weakest-link keeps an index honest about uneven signal counts (spec 3.2) instead of mixing.*

139. **Validity severity mapping.** too-fast >30%→strong else mild; same-position >60%→strong; Gs smearing→strong; idle gaps→mild; chance-level→a mild per-domain flag that lowers **that index's** confidence (not a session-wide strong flag). Graduated outcome: any strong→`not_representative`; only mild→`gentle_note`; none→`valid`. *Why: spec 7.1 names the flags and the mild/strong→note/retry split but leaves per-flag severity to us; same-position + smearing are unambiguous gaming → strong; chance-level "намалена доверба за индексот" maps to confidence, not a hard retry.*

140. **`CognitiveProfile` derived-features shape (Code owns the structure).** Structural-only (no copy): profileShape flat/spiky + spread, highest/lowest index, strong-pairs + gap-pairs, solving style (reflective/fast-accurate/fast-errors/balanced from speed×accuracy), memory forward−backward asymmetry (null under 8), Glr learning slope, ceiling/floor domains. Feature cutoffs live in `norms.FEATURES`, PROVISIONAL. *Why: spec 9.1 names the features but not the data structure or cutoffs; 3.07 maps these to prose so it stays a pure text layer.*

141. **Credit seam: an item's `judge()` returns `credit` already net of domain penalties.** For Gs, `credit` encodes `(correct − 0.5·errors)/targets`; for EF, `min_moves/moves` (+planning eff). The scoring layer reads `credit` + `meta` and never re-derives from the payload, so it stays generator-agnostic. *Why: keeps engine/scoring agnostic to how 3.04 builds items; documented in `raw.ts` as the 3.04 seam contract.*

142. **No spec-vs-plan mismatch found.** Every numeric value `plan.md` §6 restates (composite weights, band cutoffs, the B.2 formulas, the span table) matches the spec PDF appendices verbatim; nothing in `plan.md` had to be overridden by the PDF this phase. *Why: the brief required surfacing any disagreement (PDF wins) — there was none to surface.*

### 2026-06-22 — Phase 3.04 item bank (Code)

143. **3.04↔3.05 boundary as built: 3.04 = generators + content-spec DATA + answer/`judge` + scoring meta; 3.05 = the screens that render them.** `phase-plan.md`'s critical-path line lumps "task screens" under 3.04, but the live boundary (logic in 3.03/3.04, UI in 3.05) puts task-screen *rendering* in 3.05. This layer ships ZERO React/DOM/SVG/Tailwind — only typed, renderer-agnostic data. *Why: the phase brief's explicit boundary note; keeps the generators testable + reusable and lets 3.05 own look + flow + i18n against the 3.02 design.*

144. **Content-spec seam: `payload` is a discriminated `TaskSpec` union keyed by `taskType`, carrying `interaction` (the input model AS DATA) + a separated `solution`.** `solution.answer` IS the canonical correct `Response.answer`, so a single generic oracle (`correctAnswerFor`) drives a "perfect" responder and `judge` compares against it; `wrongAnswerFor` derives a guaranteed-wrong answer from `interaction.mode`. The renderer must not render `solution`. *Why: one uniform contract for 3.05 (which widget, how to package the answer) + the tests/end-to-end check, without the test hard-coding each task's internals.*

145. **Provider is the production source; `fixtures.ts` kept for engine tests.** `createTaskItemProvider()` (a thin, stateless dispatcher over the seven generators) implements the engine's `ItemProvider` + adds `getPracticeItem(domain)`. Per-domain/per-item stream independence is carried by the engine's existing `deriveSeed`-per-domain rng (no provider state). *Why: the brief — wire the real items as the source 3.05 injects, keep the deterministic stubs for the engine's own unit tests; all prior 420 tests stay green.*

146. **Glr multi-attempt = a WeakMap-memoized learning block keyed by the per-domain `rng` instance — NO seam extension needed.** Paired-associate learning needs the SAME pairs re-tested across attempts, which a per-call pure function cannot express. A module-level `WeakMap<Rng, {pairs, attempt}>` in `glr.ts` generates the pair set on the first call for a given rng and increments `attempt` on each subsequent call; the existing `ItemScoringMeta.attempt` field feeds `raw.ts learningSlope` unchanged. This is fully reproducible (each `runSession` builds a fresh per-domain rng → identical pairs from the identical stream prefix; the WeakMap auto-GCs) but is the **one documented exception** to the "pure function of inputs" contract — flagged in code + here. *Why: the brief pre-authorised a minimal additive seam change for Glr "if genuinely needed"; it was NOT needed — the existing `attempt` meta + stateful provider dispatch suffices, the smallest possible change. The review confirmed determinism is sound and this is the only stateful exception.*

147. **PROVISIONAL — Gf above the spec's level-5 series table.** Прилог A.1 tabulates series rules only to level 5 (+k → ×2 → ×k/alternating → second-order). Levels 6–10 use an alternating ×k/+k rule of growing magnitude; matrices scale by varying-attribute count (1→3, the difficulty measure) using constant + progression rules (XOR named in the spec is deferred). *Why: the spec underspecifies the high end; defensible monotonic extension, flagged for the pilot/psychologist review like 3.03's seed norms.*

148. **PROVISIONAL — Gv uses 90/180/270° rotations; 135° deferred.** Прилог A.2 mentions a 135° rotation, but the polyomino-on-grid shape model only supports 90° multiples (135° doesn't map cells onto a grid). Difficulty scales by shape cell-count (3→7) + mirror-distractor subtlety. *Why: 135° would require switching Gv stimuli to vector-path shapes — out of scope this phase; surfaced as a spec-vs-implementation gap.*

149. **PROVISIONAL — Gsm level→span map + non-consecutive tile revisits for spans > 6.** Спан = `clamp(round(3 + level·0.5), 2, 9)` (monotonic; tuned so the age-keyed start level ≈ Прилог B.1's per-age expectation). Прилог A.3 fixes the board at **6 tiles**, yet B.1 expects forward span up to **7** (age 13) — impossible with 6 distinct tiles, so sequences allow non-consecutive tile revisits (never two identical consecutive). *Why: an internal spec tension (A.3 vs B.1); resolved by revisiting the apparatus rather than changing tile count, flagged for review. Spec gives age→span, not level→span — the mapping is ours.*

150. **PROVISIONAL — Gs per-level field/target/time maps.** `cellCount = clamp(16 + 2·level, 18, 28)`, `targetCount = clamp(3 + level, 3, ⌊cellCount·0.45⌋)`, `timeBudgetMs = clamp(20000 + level·500, 20000, 25000)` — all monotonic, within Прилог A.4's by-age ranges (18–28 cells, 20–25 s). The known ~46 Gs floor artifact (#136) was left untouched (norms-calibration, not a generator bug). *Why: the spec gives by-age ranges + the credit formula, not a per-level table; defensible monotonic mapping for the 1–10 level model.*

151. **PROVISIONAL — EF minMoves cap of 7.** `targetMinMoves = clamp(level + 1, 2, 7)`. Прилог A.5 fixes the low end (minimum 2→5 by level); the cap-7 extension covers the high levels. start/goal are generated by an exhaustive BFS over the classic ToL state space (capacities [3,2,1], 3 balls) so `minMoves` is the BFS-exact shortest distance; above the largest reachable distance the generator falls back to the true max (rare). *Why: the spec underspecifies the high end; BFS-exactness keeps optimality scoring correct, and the review independently re-derived the distances.*

152. **PROVISIONAL — Glr level→pairCount + single block + 0.5 recall threshold.** Pairs by level: L1-2→4, L3-4→5, L5-6→6, L7-8→7, L9-10→8 (within Прилог A.6's "4–8 by age"). The spec says "2–3 attempts" but the engine's Glr cap is 4–6 items — we use ONE learned block recalled across all of them (richer slope data); `correct` (for the adaptive step) = recall credit ≥ 0.5 while credit stays continuous (`correctCount/pairCount`). *Why: the spec gives by-age ranges; one block maximises the learning-slope signal within the engine's existing cap; thresholds flagged for review.*

153. **CT implements 5 sub-types (incl. maze), PROVISIONAL difficulty + uniform selection + 4-option default.** Прилог A.8 tabulates 4 (sequence/debug/loop/conditional); Дел 4 also names "лавиринт" (maze) — implemented, so 5 total. Each carries its own typed spec + `judge`; a shared `[col,row]` grid simulator (up=row−1) judges sequence/maze and validates their declared solution paths. Sub-type chosen by uniform `pick(rng,…)`; difficulty grows per sub-type (path length, program length, repeat count, rule/input count); MC sub-types use 4 options (matching Gf's "Избор од 4"). *Why: maze is in the spec's task-type section though absent from A.8's table; the per-sub-type difficulty + selection + option counts are underspecified and set defensibly.*

154. **No further spec-vs-`plan.md` mismatch.** The only spec-internal tension found is A.3 (6 tiles) vs B.1 (span 7), recorded in #149; `plan.md` did not need overriding. Determinism, scoring-meta fidelity, the no-forbidden-token invariant, and renderer-agnostic typing were confirmed by a fresh-context review subagent (verdict PASS). *Why: the brief required surfacing any disagreement (PDF wins) and a fresh-context review before signing the DoD.*

---

## Phase 3.05 — the live assessment flow + task screens (Code)

155. **Dependency Gate B was WAIVED by the operator; the visual design was derived from `brand.md` §6 + `plan.md` §5/§8, not a 3.02 handover.** The phase prompt's binding visual spec (`docs/design-handovers/Part-3-Phase-02-Handover.md` + token file + prototype) **does not exist** anywhere in the repo — Part-3 phase reports jump 3.01 → 3.03, so the 3.02 design phase was never executed/committed. Gate B says "STOP and report" if absent. Reported; the operator instructed "continue", so the design was derived from the confirmed brand constants (official palette, Montserrat, puzzle-brain motif, ≥44px/spacing/radius scales) + the spec's assessment requirements. *Why: explicit operator override of the gate; logged here so it is not a silent ratification. The screens are on-brand but were NOT validated against a real 3.02 handover — if one lands later, reconcile.*

156. **`/test` now mounts the v2 flow; v1 left intact and orphaned (not deleted).** The v1 `TestRunner` + `/result` + gate + email code remain in place but are no longer reachable from `/test`; their tests stay green. *Why: the phase's Compatibility decision — keep v1 until its v2 replacements land (3.06/3.09/3.10).*

157. **Fixed a latent Phase 3.01 `globals.css` defect: the v2 brand tokens were silently dropped in `next dev`.** The `:root` block had a banner comment containing a long run of `=` characters; Turbopack's dev CSS parser truncated on it, dropping every token below it (`--iq-*`, `--index-*`, `--space-*`, `--tap-min`) in `next dev` — so the entire v2 brand palette (`bg-iq-violet`, `text-index-*`, etc.) resolved to nothing in dev (the **production** build was unaffected). Replaced the banner with a concise comment. Additive, no token value changed. *Why: without it the whole v2 UI was unstyled in dev; the prod-vs-dev split made it invisible to the 3.01 verification (no v2 UI existed then to surface it).*

158. **Bespoke SVG/inline styles use literal brand hex, not `var(--iq-*)` / `var(--index-*)`.** Those tokens live in Tailwind's `@theme inline`, whose values are inlined into utility classes and are NOT guaranteed as raw `:root` custom properties — so `var(--index-logical)` does not resolve inside an SVG `fill`. The puzzle-brain, the Explorer badge, the glyph/arrow vocabulary, and the CT accent use literal hex (`REGION_HEX`, a `VIOLET`/`ACCENT` constant, `COLOR_HEX`). Tailwind utility classes (`bg-iq-violet`, …) are used everywhere else and work (after #157). *Why: SVG `fill` needs a resolvable value; literal hex is robust in dev, prod, and static-SVG contexts.*

159. **Session seed via Web Crypto, not `Math.random`.** `generateSeed(age)` = `iqup-<age>-<crypto nonce base36>` (falls back to a time seed where crypto is absent). *Why: preserves the 3.03/3.04 no-`Math.random` source invariant; a fresh seed per session (and per retry) yields a new item set, the determinism-gives-new-tasks property the retry relies on.*

160. **Device calibration = median of 5 tap latencies, clamped [150, 2000] ms (fallback 600).** The first practice shows a "tap the circle" warm-up; the median inter-tap latency becomes `calibrationBaselineMs` (spec 7.2), carried into the `SessionRun` for calibration-relative scoring. *Why: the spec requires a device tap-baseline but not its capture method; median-of-5 + clamp is robust to a stray slow/double tap.*

161. **Direct access is handled by the in-flow age-setup screen, not a redirect.** Arriving at `/test` with no/invalid `?age` shows the flow's own age picker (5–13, **no name**); a valid `?age=N` auto-starts and skips it. *Why: friendlier than the v1 redirect, and age setup is in-scope for 3.05; the guard ("no valid age → can't reach a task") is satisfied structurally since the flow collects age before any task.*

162. **Practice is unscored and skippable; the example is the real renderer.** Each new domain's practice renders `getPracticeItem(domain)` via the real `TaskRenderer` with a no-op `onAnswer`; a "ready" button proceeds. *Why: the child sees the actual interaction; practice answers must never enter the `SessionRun`.*

163. **Dev autopilot is component state (`finish` / `force retry` / `stop`).** A non-production QA control that fast-advances the whole flow (correct answers at ~600 ms, or too-fast to force the not-representative/retry path). Stripped in prod by the server `dev` gate. *Why: the phase's optional dev preview; kept as state (not a ref) so toggling re-runs the driver effect.*

164. **The Explorer badge entrance is scale-only (no opacity-0 initial).** Essential reward content must never depend on an opacity animation that may not run (reduced motion, a backgrounded tab, throttled rAF) — the badge animates `scale` only and rests fully visible. *Why: surfaced in verification — an opacity-0 initial left the badge invisible when the animation engine was paused.*

---

## Phase 3.06 — the parent form + the two-store data model (Code)

165. **Form integration = a dedicated route `/report`, not an in-flow `AssessmentFlow` step.** The completion screen's "continue to your report" (`onContinue`) now `router.push('/report')`; `/report` is an SSG shell + a `ReportFlow` client island that reads the persisted `iqup.assessmentRun.v1`, recomputes the profile client-side, and captures the parent. *Why: mirrors the existing `/result` / `/trial` / `/privacy` SSG-shell-plus-island pattern, keeps the large assessment island focused, gives the form its own route/guard/metadata, and lets 3.09 replace the interstitial in place. (The IA in `plan.md` §3 reserves `/result` for the eventual v2 summary; v1 `/result` is still mounted/orphaned this phase, so `/report` avoids the collision — a later cleanup can reconcile.)*

166. **Store A numeric columns are `double precision`, not `numeric`.** *Why: PostgREST returns `numeric`/`decimal` as strings (to preserve arbitrary precision) but `double precision`/`real` as JS numbers; the 0–100 signal/index scores fit float8 exactly enough and arrive typed as `number` through the typed client, so the row round-trips without string coercion.*

167. **Store A `validity` collapses the 3-outcome validity enum to a 2-value flag.** `gentle_note` (a mild flag → still a representative session) maps to `valid`; only `not_representative` maps to `not_representative`. *Why: the spec's Store A `validity` column is `valid`/`not_representative` (so norm calibration can exclude non-representative runs); `gentle_note` is representative. In practice the form is only reachable on `valid`/`gentle_note` (the `not_representative` outcome routes to retry, never persists the hand-off), so the stored value is `valid` today — the mapping is kept explicit for forward-compatibility.*

168. **Store A `city` = the centre slug id (machine key); Brevo `CITY` = the centre's English city label.** *Why: Store A wants a stable machine key for aggregation; Brevo wants a human, locale-independent value for campaign segmentation. Using different representations also reinforces unlinkability — neither is a unique join key (city is a coarse demographic bucket either way).*

169. **The two writes: Brevo lead = PRIMARY, inline-isolated, non-trapping + recoverable-log on failure; Store A = isolated, non-blocking via `after()`.** The action checks the honeypot first (no writes for bots), re-validates the lead with `assessmentLeadSchema`, schedules `insertAnonymousScore` in `after()` (so it never delays the reveal), and awaits `upsertAssessmentLead` inside an `isolate` (never throws). On a Brevo failure the orchestrator logs the FULL lead under a `lead-recover` marker so it is not silently lost. *Why: the phase's isolation contract — results reveal even if either write fails; the primary capture is attempted before returning but can't trap the parent; durability hardening (retry/queue) is explicitly deferred — `// TODO(durability 3.16)`.*

170. **The form is genuinely non-trapping — no general error banner.** Results recompute client-side from the persisted run, so on submit the island reveals the interstitial on success AND on any thrown/`{ok:false}` action result (logging the failure). Only field-level validation errors (required fields, email format) block submission, with focus moved to the first invalid field. *Why: the hard rule is "results must reveal even if any server write fails"; a blocking error banner would contradict it, so it was removed rather than left as dead code.*

171. **Optional child-gender value set = `female` / `male` / `unspecified`, plus a "prefer not to say" choice that stores `null`.** The DB column is nullable with `check (gender is null or gender in ('female','male','unspecified'))`. *Why: the spec asks for a minimal set and flags it for IqUp confirmation; "prefer not to say" (→ null) and an explicit "unspecified" both exist so a parent can either skip or actively decline. FLAGGED for IqUp confirmation.*

172. **`CHILD_GENDER` is omitted from the Brevo attribute payload when gender is null** (rather than sent as empty). *Why: an upsert with `updateEnabled` would otherwise clear an existing value on a re-take; omitting the key leaves it untouched.*

173. **New v2 lead-context key `iqup.leadContext.v2` = `{parentFirstName, city, submittedAt}`.** Written on a successful submit, read by 3.09 to gate + greet + drive the nearest-centre trial CTA. *Why: separate from the v1 `iqup.leadContext.v1` (different shape: no child name, adds city); carries no email and no scores — only what the results chrome legitimately needs. The numbers 3.09 reveals are recomputed from `iqup.assessmentRun.v1`, not duplicated here.*

174. **`CONSENT_VERSION_V2 = 'v2-draft-2026-06'`, `SOURCE = 'website-assessment'`, and the `TOP_INDEX` English label set are new v2 constants.** The consent set grew from 2 (v1) to 3 (process + guardian required, marketing optional), so the version is bumped. *Why: re-versioning is required when consent wording changes; `TOP_INDEX` (e.g. "Logical thinking") is the single coarse result-derived Brevo attribute — no numbers/bands. The draft consent copy + wording are FLAGGED for IqUp legal.*

175. **`/report` is `robots: {index:false, follow:false}`.** *Why: a post-funnel capture step with no shareable public content; it should not be indexed (the form requires a completed in-memory run to function anyway).*

176. **The `assessment_scores` migration was NOT applied live, and live Store A + Brevo verification are DEFERRED-pending-keys.** The `.env.local` on this machine is the blank template (no Supabase URL/service-role key, no `BREVO_API_KEY`), and the sandbox can't reach Postgres, so the migration must be applied via `npm run db:push` (once linked) or the Supabase dashboard SQL editor, and the live RLS proof (`npm run test:scores`) + Brevo upsert run once Cowork's keys land. The migration mirrors the proven v1 `leads` security model exactly, and the schema/security logic is covered by unit tests + the browser end-to-end run (which confirmed the Brevo no-key no-op and the isolated Store A failure both leave the parent revealing results). *Why: matches the phase's anticipated "live Brevo/Supabase verification pending Cowork's keys" carryover.*

---

## Phase 3.07 — the report engine (Code)

177. **The `ReportContent` output contract lives in `src/lib/report/types.ts`; the localised module library lives in `src/content/report/`.** The contract gained two sections beyond the brief's enumerated list — **`overview`** (the profile-shape + index-pair sentences) and **`extremes`** (ceiling/floor) — so every taxonomy category in the brief has an explicit home in the typed output. *Why: the brief says "finalise exact field names against the real upstream types" and lists profile-shape + index-pair + extremes as required module families without a named output slot; giving each its own field keeps `buildReport` a flat, render-ready object rather than smuggling them into another section.*

178. **The generated date is CALLER-SUPPLIED via `context.generatedAt` (an ISO string), day-level-truncated by string-slice — the engine never reads the clock.** When omitted, `meta.generatedDate` is `null`. *Why: the DoD requires byte-identical output for the same profile + context AND a day-level date in `meta`; reading `Date.now()` would break determinism. Making the instant a context input keeps `buildReport` pure (the send-time is the caller's, 3.10), and slicing the `YYYY-MM-DD` prefix instead of `new Date(...)` avoids any timezone shift.*

179. **Age → program is a clean, non-overlapping partition of 5–13 over four programs: 5–7 → Magic Laboratory · 8–9 → Magic Laboratory PLUS · 10–11 → Oliver's Scientific Adventures · 12–13 → Oliver's Scientific Adventures PLUS.** "Little Explorers" (3–5) is excluded. Implemented as the `AGE_TO_PROGRAM` data record so a correction is a one-line edit. **FLAGGED for IqUp to confirm.** *Why: the five brand programs have overlapping ranges; the report needs exactly one program per exact age. This monotonic partition keeps every age inside that program's real brand range and respects the base→PLUS progression; the assessment starts at 5, where Magic Laboratory is the natural school-age STEM entry, so Little Explorers is dropped.*

180. **The learning-slope is bucketed into copy labels (`fast` ≥ 0.2 · `repetition` ≤ 0 · `steady` between) in the report layer, with PROVISIONAL thresholds.** This is treated as **presentational selection**, not a recomputed psychometric. *Why: the slope value is derived upstream in `CognitiveProfile.features.learningSlope` and only mapped to a sentence here — the same kind of presentational mapping the spec explicitly assigns to this layer (band→word). The cutoffs are flagged PROVISIONAL alongside the other seed-norm thresholds for the pilot/psychologist re-tune.*

181. **`not_representative` produces the FULL report plus a `caveated: true` flag and a strong lead caveat note — the engine never withholds; whether to send is the caller's decision (3.10).** `gentle_note` adds a soft note (`caveated: false`); `valid` adds none. *Why: the brief says the engine must *support* the caveated variant and that the send/withhold decision belongs to the caller. Emitting a clearly-flagged caveated report (rather than a stripped one) lets 3.10 choose to email it, route to retry, or hold — without the engine making policy.*

182. **Home activities are drawn for the index priority `[growth, top-strength, a STEM-relevant (or second-highest)]` — one activity per index, with which of a cell's ≥2 equivalent activities chosen by a seed-driven rotation (`(seedInt + ordinal) % len`).** *Why: the brief wants 2–3 activities keyed by (index, age-cluster) and a strong "two children rarely get the same report" property. Anchoring on growth + strength + STEM keeps the picks meaningful; the deterministic seed rotation varies the exact wording between children without breaking determinism for a given profile.*

183. **The forbidden-token scanner bans clinical / diagnostic terms everywhere EXCEPT the disclaimer + validity notes, where they may appear only in negation; all other tokens (digits, `%`, IQ, score, rank, "level N", "below average", "weak") are banned everywhere including the disclaimer.** *Why: this resolves the brief's apparent internal conflict — "no clinical or diagnostic terms" vs. "the disclaimer states the profile is indicative, **not diagnostic** … never 'clinical IQ'." The disclaimer's whole job is to negate those words; scoping the clinical-term exception to it (and positively asserting the negation is present) keeps the guardrail non-vacuous everywhere else.*

184. **Scope is bilingual MK (default) + EN (mirror) only — not the 3.01 scaffold README's `mk·sr·hr·en`.** The engine is locale-generic (keys off the `Locale` type), so adding a locale later is a data-only edit; both placeholder READMEs were corrected. Two smaller calls ride along: index-pair narration is capped at the first **two** distinct pair variants (avoids a wall of pairs), and **`context.gender` is accepted but unused** — copy is gender-neutral by default in MK, with gender reserved for a future agreement pass. *Why: the phase brief, the live `Locale = 'mk' | 'en'` type, and every sibling content module are MK+EN; the scaffold's four-locale note predated that decision. Gender-neutral-by-default avoids MK grammatical-gender assumptions on a child whose gender is optional.*

---

## Phase 3.09 — the on-screen results screen (Code)

185. **The 3.08 design deliverables were landed on `main` in a separate commit before 3.09 branched** (handover + `surfaces/` + `report-kit.js` + `tokens-v2.css` + mockups + 3.08 completion, under `docs/design-handovers/`). They had been produced in a Cowork session and never committed; Task 0's dependency gate requires 3.08 on `main`. *Why: keeps the 3.09 branch diff "only additive results-screen work" (the DoD's clean-diff check) and mirrors how 3.05–3.07 each landed on `main` first. Lazar approved the main commit.*

186. **The v2 SEMANTIC token layer was lifted into `globals.css` (additive).** The 3.09 prompt assumed `--ix-*` / `--action` lived in `globals.css`, but only the 3.01 PRIMITIVES (`--iq-*`, `--index-*`, `--space-*`, `--tap-min`, `--radius-*`) were ever added — the 3.02 `tokens-v2.css` semantic roles (`--ix-*` `-soft/-tint/-ink` ramps, `--action*`, `--band-*`, `--surface-2`, `--ink-head/-muted`, `--neutral`, `--line*`, `--r-*`, `--tap-comfort`, `--elev-*`, `--focus*`, `--dur-fast`) were never lifted. `--ix-logic`→`var(--iq-magenta)` etc. keep the official hues single-sourced; only genuinely-new ramp/ink/surface values carry literals, copied from the locked `tokens-v2.css`. *Why: porting Surface A faithfully without hardcoding hex (forbidden) required these tokens to exist; using the design's own locked values is "no new identity."*

187. **The results-screen CSS is a `.iq-results`-scoped block in `globals.css`, not a CSS Module.** The React island is therefore pure TSX with className strings (no CSS import). *Why: matches the repo's single-stylesheet pattern AND lets the island render under Vitest's Node env via `react-dom/server` for the rendered-screen forbidden-token test (a `.module.css` import would need a CSS stub in the Node test env).*

188. **The IqUp wordmark + MK/EN toggle are NOT re-rendered inside the results screen** (handover §2.1 lists them in the header) — they are already provided by the global `SiteHeader` on `/report`. The results header renders only the title + age/date meta + the wave. *Why: re-adding them would duplicate the page chrome; the app context already satisfies that part of the handover.*

189. **The index card drops the mock's separate "plain description" line — the real `ReportIndex` has no per-index description field** (only `name`, `band`/`bandLabel`, `confidence`/`confidenceLabel`, `confidenceNote`). The card = icon + name + band-WORD pill + a confidence row (the confidence WORD + the non-numeric 3-pip mark + `confidenceNote` as the sentence). *Why: the prompt said "all from `ReportContent.indices[]`", but the kit's SAMPLE had a `desc` the real 3.07 contract never emits; the card consumes only real fields rather than inventing content.*

190. **`not_representative` is rendered defensively (for tests + PDF/3.10 parity), not because it is reachable via the funnel.** The 3.05 flow routes `not_representative` to `RetryScreen` and does NOT persist the run, so the form/results are only reachable on `valid`/`gentle_note`. On the results screen the state renders the bespoke amber caveat + a DIMMED identity pentagon (`dim` = all five wedges at 0.32 opacity, still whole/same-size — handover §1) + a single retry link to `/test` that mirrors `RetryScreen`'s restart (never a contradictory second retry). *Why: the prompt requires all three validity states to render + be tested; the dim follows the handover prose even though the reference `Results.html` mock didn't wire it.*

191. **`bookingUrlFor(locale, cityKey)` falls back to the localized `/trial` (not `/booking`) when `NEXT_PUBLIC_BOOKING_URL` is unset**, always appending `?grad=<centre-id>` (the stable slug, never a label, the only thing in the URL). *Why: the prompt's explicit instruction (reuse `trialBookingUrl`) — so the CTA is never a dead link before the real IqUp booking URL lands. New env var documented in `.env.local.example` + `00_stack-and-config.md`.*

192. **AA contrast fix (axe-confirmed): the confidence label + disclaimer use `--ink-muted` (#5A6675), not `--ink-faint` (#8a8499).** axe flagged `--ink-faint` at 11px-bold / 12px as 3.59:1 (< 4.5:1). *Why: `tokens-v2.css` itself reserves `--ink-faint` for "large / UI / placeholder only"; this small body-ish text needs the AA-clearing `--ink-muted`. Results screen is now axe-clean in both locales (16 passes, 0 violations), incl. the validity states by token reuse.*

193. **Two supporting config edits ride along:** `eslint.config.mjs` ignores `docs/**` (the vendored 3.08 reference surfaces incl. `report-kit.js` are standalone browser JS to PORT, not app source), and `vitest.config.ts`'s include grew to `*.test.{ts,tsx}` for the results-screen render test. The `/report` page container widened `max-w-2xl` → `max-w-[1080px]` so the results' ≥880px two-column layout isn't capped (the form self-centres at `max-w-md`, unaffected). *Why: keep lint/build green over vendored references and enable the rendered-screen test without touching app behaviour.*
