# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# QA tooling output (Phase 1.11) — dev-only, regenerated on demand
/.lighthouseci/
/.lhtmp/
/.nextstart.log
/.lighthouseci*.log
/.lhmedian.log
/.pw*.log
/test-results/
/playwright-report/
/playwright/.cache/

# Phase 3.10 PDF eyeball samples (dev-only, regenerated via `npm run report:sample`;
# PDFs embed Montserrat and are large — not committed)
/docs/qa/Part-3-Phase-10/

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# ...but DO commit the placeholder template (no secrets in it).
!.env.local.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
